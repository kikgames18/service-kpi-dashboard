# 📤 Инструкция по публикации на GitHub

## Шаг 1: Установка Git (если не установлен)

Если Git не установлен, скачайте и установите:
- Скачать: https://git-scm.com/download/win
- После установки перезапустите терминал

## Шаг 2: Проверка установки Git

Откройте PowerShell или командную строку и выполните:
```bash
git --version
```

Должна отобразиться версия Git (например, `git version 2.43.0`)

## Шаг 3: Настройка Git (первый раз)

Если вы используете Git впервые, настройте имя и email:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

## Шаг 4: Инициализация репозитория

Откройте PowerShell в папке проекта и выполните:

```bash
# Перейдите в папку проекта (если еще не там)
cd "c:\Users\German\Downloads\project-bolt-sb1-qsjjhlj4 (1)\project"

# Инициализируйте Git репозиторий
git init
```

## Шаг 5: Добавление файлов

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что добавлено (опционально)
git status
```

## Шаг 6: Первый коммит

```bash
git commit -m "Initial commit: KPI Dashboard for Service Business"
```

## Шаг 7: Создание репозитория на GitHub

1. Откройте браузер и перейдите на https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**

5. Заполните форму:
   - **Repository name**: `service-kpi-dashboard` (или другое имя на ваш выбор)
   - **Description**: "Веб-дашборд для управления KPI сервисного центра 'Сервис всем'"
   - Выберите **Public** (публичный) или **Private** (приватный)
   - ⚠️ **НЕ ставьте галочки** на:
     - ❌ "Add a README file" (у нас уже есть)
     - ❌ "Add .gitignore" (у нас уже есть)
     - ❌ "Choose a license" (можно добавить позже)

6. Нажмите кнопку **"Create repository"**

## Шаг 8: Подключение к GitHub

После создания репозитория GitHub покажет инструкции. Выполните в PowerShell:

```bash
# Замените YOUR_USERNAME и YOUR_REPO_NAME на ваши данные
# Например: git remote add origin https://github.com/german/service-kpi-dashboard.git

git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**Пример:**
Если ваш username `german` и репозиторий `service-kpi-dashboard`, команда будет:
```bash
git remote add origin https://github.com/german/service-kpi-dashboard.git
git branch -M main
git push -u origin main
```

## Шаг 9: Авторизация на GitHub

При первом `git push` GitHub может запросить авторизацию:

### Вариант 1: Personal Access Token (рекомендуется)

1. Перейдите на: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: "KPI Dashboard Project"
   - **Expiration**: выберите срок действия
   - **Scopes**: отметьте `repo` (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)
6. При запросе пароля в Git используйте этот токен вместо пароля

### Вариант 2: GitHub Desktop

Можно использовать GitHub Desktop для визуальной работы с Git:
- Скачать: https://desktop.github.com/

## Шаг 10: Проверка

После успешного push откройте ваш репозиторий на GitHub:
```
https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
```

Вы должны увидеть все файлы проекта!

## 🔄 Обновление репозитория (после изменений)

Когда вы вносите изменения в проект:

```bash
# Проверьте статус изменений
git status

# Добавьте измененные файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание того, что было изменено"

# Отправьте изменения на GitHub
git push
```

## 📋 Что будет в репозитории

### ✅ Включено:
- Все исходные файлы (`.ts`, `.tsx`, `.js`, `.sql`)
- Конфигурационные файлы
- Документация (README.md, QUICKSTART.md и т.д.)
- Примеры конфигурации (`.env.example`)

### ❌ НЕ включено (благодаря .gitignore):
- `node_modules/` - зависимости (слишком большие)
- `.env` файлы - секретные данные (пароли, ключи)
- `dist/` и `build/` - собранные файлы
- Логи и временные файлы

## ⚠️ Важные замечания

1. **НЕ коммитьте файлы с секретами:**
   - `server/.env` - содержит пароли к БД
   - `.env` - содержит настройки

2. **Эти файлы уже защищены .gitignore**, но проверьте перед push:
   ```bash
   git status
   # Убедитесь, что .env файлы не в списке
   ```

3. **Если случайно добавили секреты:**
   - Немедленно измените пароли/ключи
   - Удалите файлы из истории Git (требует дополнительных действий)

## 🎯 Быстрая команда для первого раза

Если все настроено, можно выполнить все сразу:

```bash
cd "c:\Users\German\Downloads\project-bolt-sb1-qsjjhlj4 (1)\project"
git init
git add .
git commit -m "Initial commit: KPI Dashboard for Service Business"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

## 📞 Помощь

Если возникли проблемы:

1. **Ошибка "remote origin already exists"**
   ```bash
   git remote remove origin
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   ```

2. **Ошибка авторизации**
   - Используйте Personal Access Token вместо пароля
   - Или используйте GitHub Desktop

3. **Файлы не загружаются**
   - Проверьте `.gitignore` - возможно файлы игнорируются
   - Проверьте размер файлов (GitHub имеет лимиты)

---

**Готово!** После выполнения этих шагов ваш проект будет на GitHub! 🚀

