# Дашборд KPI для "Сервис всем"

Веб-дашборд для разработки и визуализации KPI для предприятия "Сервис всем" (ремонт компьютерной и бытовой техники).

## 🚀 Технологии

- **Frontend**: React 18 + TypeScript + Vite 5 + Tailwind CSS
- **Backend**: Node.js 24.11.00 + Express
- **База данных**: PostgreSQL 18
- **Аутентификация**: JWT (JSON Web Tokens)

## 📋 Требования

### Обязательные компоненты:

1. **Node.js v24.11.00 или выше**
   - Скачать: https://nodejs.org/
   - Проверка: `node --version`

2. **PostgreSQL сервер (версия 18 или выше)**
   - ⚠️ **Важно**: pgAdmin 4 - это только клиент для управления
   - Нужен сам PostgreSQL сервер (обычно устанавливается вместе с pgAdmin)
   - Скачать: https://www.postgresql.org/download/
   - Проверка: откройте pgAdmin → если видите серверы, значит PostgreSQL установлен

3. **pgAdmin 4** (для управления базой данных)
   - Скачать: https://www.pgadmin.org/download/

4. **npm** (обычно идет с Node.js)
   - Проверка: `npm --version`

### Как проверить, установлен ли PostgreSQL сервер:

1. Откройте pgAdmin 4
2. Если в левой панели вы видите серверы (например, "PostgreSQL 18"), значит сервер установлен ✅
3. Если серверов нет, нужно установить PostgreSQL отдельно

## 📦 Установка и запуск

### Шаг 1: Клонирование репозитория

```bash
git clone <ваш-repo-url>
cd project
```

### Шаг 2: Настройка базы данных

1. Откройте pgAdmin 4 и подключитесь к серверу PostgreSQL
2. Создайте новую базу данных с именем `service_kpi`:
   - Правый клик на "Databases" → Create → Database
   - Имя: `service_kpi`
3. Выполните миграции базы данных:

   **Вариант 1: Автоматически (рекомендуется)**
   ```bash
   cd server
   npm install
   # Создайте файл .env (см. ниже)
   node run-migrations.js
   ```

   **Вариант 2: Вручную через pgAdmin**
   - Выберите базу данных `service_kpi`
   - Выполните SQL скрипты в следующем порядке:
     - `server/migrations/01_create_schema_fixed.sql`
     - `server/migrations/complete_setup.sql`

### Шаг 3: Настройка бэкенда

1. Перейдите в папку `server`:
   ```bash
   cd server
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env`:
   ```bash
   # Windows PowerShell
   @"
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=service_kpi
   DB_USER=postgres
   DB_PASSWORD=ваш_пароль_postgres
   PORT=3001
   NODE_ENV=development
   JWT_SECRET=ваш_секретный_ключ_для_jwt_измените_в_продакшене
   "@ | Out-File -FilePath .env -Encoding utf8
   ```

   Или создайте файл `.env` вручную со следующим содержимым:
   ```
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=service_kpi
   DB_USER=postgres
   DB_PASSWORD=ваш_пароль_postgres
   PORT=3001
   NODE_ENV=development
   JWT_SECRET=ваш_секретный_ключ_для_jwt_измените_в_продакшене
   ```

4. Запустите бэкенд-сервер:
   ```bash
   npm start
   ```

   Или в режиме разработки с автоперезагрузкой:
   ```bash
   npm run dev
   ```

   Бэкенд будет доступен по адресу: `http://localhost:3001`

### Шаг 4: Настройка фронтенда

1. Вернитесь в корневую папку проекта:
   ```bash
   cd ..
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` в корне проекта:
   ```
   VITE_API_URL=http://localhost:3001/api
   ```

4. Запустите фронтенд-сервер:
   ```bash
   npm run dev
   ```

   Фронтенд будет доступен по адресу: `http://localhost:5173`

### Шаг 5: Добавление тестовых данных (опционально)

Для добавления тестовых данных (5 техников, 5 заказов, 5 записей KPI):
```bash
cd server
node seed-data.js
```

## 🔐 Авторизация

### Тестовые данные для входа:

- **Email**: `admin@service.ru`
- **Пароль**: `admin123`
- **Роль**: Администратор

При первом запуске бэкенд автоматически создаст тестового пользователя-администратора.

## ✨ Функционал

### 🔑 Система авторизации
- ✅ Регистрация новых пользователей
- ✅ Вход в систему
- ✅ Две роли: **Администратор** и **Пользователь**
- ✅ JWT токены для безопасной аутентификации

### 📊 Панель KPI
- ✅ Отображение ключевых метрик:
  - Всего заказов
  - Завершенные заказы
  - Выручка за день
  - Среднее время выполнения
  - Удовлетворенность клиентов
  - Отмененные заказы
- ✅ Круговые диаграммы (Donut Chart) для статусов заказов
- ✅ Линейные графики для динамики выручки
- ✅ Круговые диаграммы (Pie Chart) для распределения времени выполнения

### 📋 Управление заказами
- ✅ Просмотр списка всех заказов
- ✅ **Создание** новых заказов (только для администраторов)
- ✅ **Редактирование** заказов (только для администраторов)
- ✅ **Удаление** заказов (только для администраторов)
- ✅ Поиск по номеру заказа, имени клиента или телефону
- ✅ Фильтрация по статусу (Ожидает, В работе, Завершено, Отменено)
- ✅ Отображение информации:
  - Номер заказа
  - Данные клиента
  - Тип устройства, бренд, модель
  - Описание проблемы
  - Статус и приоритет
  - Стоимость (предварительная и финальная)
  - Назначенный техник
  - Даты получения и завершения

### 👥 Управление сотрудниками
- ✅ Просмотр списка всех техников/сотрудников
- ✅ **Создание** новых сотрудников (только для администраторов)
- ✅ **Редактирование** сотрудников (только для администраторов)
- ✅ **Удаление** сотрудников (только для администраторов)
- ✅ Отображение информации:
  - ФИО
  - Специализация (Компьютеры, Бытовая техника, Мобильные устройства, Универсальный)
  - Дата найма
  - Статус (Активен/Неактивен)
- ✅ Статистика: всего сотрудников, активных, неактивных

### 📈 Аналитика
- ✅ Диаграммы по типам устройств (Bar Chart)
- ✅ Круговые диаграммы по приоритетам заказов (Donut Chart)
- ✅ Выручка по типам устройств (Bar Chart)
- ✅ График заказов за последние 7 дней (Line Chart)

### ⚙️ Настройки
- ✅ Страница настроек (доступна администраторам)

## 📁 Структура проекта

```
project/
├── server/                 # Backend (Node.js + Express)
│   ├── routes/            # API маршруты
│   │   ├── auth.js       # Авторизация
│   │   └── data.js       # CRUD операции для данных
│   ├── migrations/        # Миграции базы данных
│   │   ├── 01_create_schema_fixed.sql
│   │   ├── complete_setup.sql
│   │   └── ...
│   ├── db.js             # Подключение к PostgreSQL
│   ├── auth.js           # JWT аутентификация
│   ├── server.js         # Главный файл сервера
│   ├── run-migrations.js # Скрипт для выполнения миграций
│   ├── seed-data.js      # Скрипт для добавления тестовых данных
│   └── package.json      # Зависимости бэкенда
│
├── src/                   # Frontend (React + TypeScript)
│   ├── components/       # React компоненты
│   │   ├── charts/      # Компоненты диаграмм
│   │   ├── CreateOrderModal.tsx
│   │   ├── CreateTechnicianModal.tsx
│   │   ├── EditOrderModal.tsx
│   │   ├── EditTechnicianModal.tsx
│   │   ├── Dashboard.tsx
│   │   ├── LoginPage.tsx
│   │   └── ...
│   ├── pages/           # Страницы приложения
│   │   ├── DashboardPage.tsx
│   │   ├── OrdersPage.tsx
│   │   ├── AnalyticsPage.tsx
│   │   ├── TechniciansPage.tsx
│   │   └── SettingsPage.tsx
│   ├── contexts/         # React контексты
│   │   └── AuthContext.tsx
│   ├── lib/             # Утилиты и API клиент
│   │   └── api.ts       # API клиент для работы с бэкендом
│   └── main.tsx         # Точка входа
│
├── supabase/
│   └── migrations/       # Исходные миграции (для справки)
│
├── .env                  # Переменные окружения фронтенда
├── .env.example          # Пример переменных окружения
├── package.json          # Зависимости фронтенда
├── vite.config.ts        # Конфигурация Vite
└── README.md             # Этот файл
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация нового пользователя
- `GET /api/auth/me` - Получить текущего пользователя

### Заказы
- `GET /api/data/orders` - Получить список всех заказов
- `POST /api/data/orders` - Создать новый заказ (только админ)
- `PUT /api/data/orders/:id` - Обновить заказ (только админ)
- `DELETE /api/data/orders/:id` - Удалить заказ (только админ)

### Сотрудники
- `GET /api/data/technicians` - Получить список всех техников
- `POST /api/data/technicians` - Создать нового техника (только админ)
- `PUT /api/data/technicians/:id` - Обновить техника (только админ)
- `DELETE /api/data/technicians/:id` - Удалить техника (только админ)

### KPI метрики
- `GET /api/data/kpi-metrics` - Получить метрики KPI

### Профиль
- `GET /api/data/profile` - Получить профиль текущего пользователя

### Health check
- `GET /health` - Проверка работоспособности сервера и подключения к БД

## 🛠️ Разработка

### Запуск в режиме разработки

**Бэкенд:**
```bash
cd server
npm run dev
```

**Фронтенд:**
```bash
npm run dev
```

### Сборка для продакшена

**Фронтенд:**
```bash
npm run build
```
Собранные файлы будут в папке `dist/`

**Бэкенд:**
```bash
cd server
npm start
```

## 🐛 Решение проблем

### Ошибка "Failed to fetch" при входе

1. Убедитесь, что бэкенд-сервер запущен на порту 3001
2. Проверьте, что в файле `.env` фронтенда указан правильный `VITE_API_URL`
3. Убедитесь, что база данных PostgreSQL запущена и доступна
4. Проверьте параметры подключения к БД в `server/.env`
5. Проверьте окно PowerShell с бэкендом на наличие ошибок

### Ошибка подключения к базе данных

1. Проверьте, что PostgreSQL сервер запущен
2. Убедитесь, что база данных `service_kpi` создана
3. Проверьте параметры в `server/.env`:
   - `DB_HOST` (обычно `localhost`)
   - `DB_PORT` (обычно `5432`)
   - `DB_NAME` (должно быть `service_kpi`)
   - `DB_USER` (обычно `postgres`)
   - `DB_PASSWORD` (ваш пароль PostgreSQL)

### ERR_CONNECTION_REFUSED на фронтенде

1. Убедитесь, что фронтенд-сервер запущен (`npm run dev`)
2. Проверьте, что порт 5173 не занят другим приложением
3. Проверьте окно PowerShell с фронтендом на наличие ошибок

### ERR_CONNECTION_REFUSED на бэкенде

1. Убедитесь, что бэкенд-сервер запущен (`npm start` в папке `server`)
2. Проверьте, что порт 3001 не занят другим приложением
3. Проверьте окно PowerShell с бэкендом на наличие ошибок

## 📝 Скрипты

### Бэкенд (server/package.json)
- `npm start` - Запуск сервера
- `npm run dev` - Запуск в режиме разработки с автоперезагрузкой

### Фронтенд (package.json)
- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка для продакшена
- `npm run preview` - Просмотр собранной версии
- `npm run lint` - Проверка кода линтером
- `npm run typecheck` - Проверка типов TypeScript

### Утилиты
- `server/run-migrations.js` - Выполнение миграций базы данных
- `server/seed-data.js` - Добавление тестовых данных
- `server/check-db.js` - Проверка состояния базы данных

## 🔒 Безопасность

- Все API endpoints (кроме `/health`) требуют аутентификации через JWT токен
- Пароли хешируются с помощью bcrypt
- JWT токены имеют срок действия 7 дней
- Роли пользователей проверяются на бэкенде
- Административные операции доступны только администраторам

## 📄 Лицензия

Этот проект создан для внутреннего использования предприятия "Сервис всем".

## 👨‍💻 Разработка

Для разработки используйте:
- Frontend: `npm run dev` (запускает Vite dev server с hot reload)
- Backend: `npm run dev` в папке `server` (запускает с автоперезагрузкой)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в окнах PowerShell (бэкенд и фронтенд)
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены
4. Проверьте, что база данных настроена правильно

---

**Версия**: 1.0.0  
**Последнее обновление**: Ноябрь 2025
