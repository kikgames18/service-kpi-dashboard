# Требования для работы проекта

## ✅ Что у вас уже есть:

1. **Node.js v24.11.00** - установлено
2. **pgAdmin 4** - установлено

## ⚠️ Что нужно проверить/установить:

### 1. PostgreSQL Сервер (КРИТИЧНО!)

**Важно:** pgAdmin 4 - это только графический клиент для управления PostgreSQL. 
Для работы приложения нужен сам **PostgreSQL сервер**.

#### Как проверить, установлен ли PostgreSQL сервер:

1. Откройте pgAdmin 4
2. Посмотрите в левую панель
3. Если видите серверы (например, "PostgreSQL 15", "PostgreSQL 16" или "localhost"), значит сервер установлен ✅
4. Если серверов нет, нужно установить PostgreSQL ⚠️

#### Если PostgreSQL не установлен:

**Windows:**
1. Скачайте PostgreSQL с официального сайта: https://www.postgresql.org/download/windows/
2. Или используйте установщик, который идет вместе с pgAdmin (если устанавливали полный пакет)
3. При установке запомните:
   - Порт (обычно 5432)
   - Пароль для пользователя `postgres` (это понадобится для настройки)

**Альтернатива:** Если у вас установлен pgAdmin через установщик PostgreSQL, сервер уже должен быть установлен.

### 2. Проверка работы PostgreSQL

1. Откройте pgAdmin 4
2. Подключитесь к серверу (обычно нужно ввести пароль)
3. Создайте базу данных `service_kpi`
4. Если это работает - PostgreSQL установлен и работает ✅

## 📋 Полный список требований:

- ✅ Node.js v24.11.00+ (у вас есть)
- ⚠️ PostgreSQL сервер (нужно проверить)
- ✅ pgAdmin 4 (у вас есть)
- ✅ npm (обычно идет с Node.js)

## 🚀 Быстрая проверка готовности:

Выполните в терминале:

```bash
# Проверка Node.js
node --version
# Должно показать: v24.11.0 или выше

# Проверка npm
npm --version
# Должно показать версию npm

# Проверка PostgreSQL (если установлен)
psql --version
# Если команда не найдена, это нормально - используйте pgAdmin
```

## 📝 Следующие шаги:

1. Убедитесь, что PostgreSQL сервер установлен и запущен
2. Создайте базу данных `service_kpi` в pgAdmin
3. Выполните миграции (SQL скрипты)
4. Настройте `.env` файлы
5. Запустите бэкенд и фронтенд

Подробные инструкции в файле `README.md`







